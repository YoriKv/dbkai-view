"""Qt front end: the main window, the session its panels and viewport share,
the export commands, the preference store and the theme.

This package and :mod:`dbkai.app` are the only places Qt may be imported.
"""
